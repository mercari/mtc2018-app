import 'package:flutter/material.dart';

class SessionDetailPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("セッション詳細")
      ),
      body: Center(child: Text('Super Session'))
    );
  }

}
